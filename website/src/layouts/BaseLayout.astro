---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { href } from "../lib/url";

interface Props {
  title: string;
  description?: string;
  /** Keep the page out of search indexes — for unlisted pages shared by link. */
  noindex?: boolean;
  /** og:type — pages default to "website"; articles pass "article". */
  ogType?: string;
  /** Page-specific social card (site-relative path under public/); falls back to the site-wide card. */
  image?: string;
}

const {
  title,
  description = "The toolkit for building code-heavy n8n workflows — agent-first, MCP-native.",
  noindex = false,
  ogType = "website",
  image = "/og.png",
} = Astro.props;

// Absolute URL for the social card (needs Astro.site set); 1200×630.
const ogImage = Astro.site ? new URL(href(image), Astro.site) : undefined;
---

<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {/* The site deploys under a path on a shared origin, so a repo-owned
        robots.txt can't reach the crawler — this meta is the real opt-out. */}
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <link rel="icon" type="image/svg+xml" href={href("/favicon.svg")} />
    <link rel="sitemap" href={href("/sitemap-index.xml")} />
    <link rel="alternate" type="text/plain" title="llms.txt" href={href("/llms.txt")} />
    <link rel="alternate" type="text/plain" title="llms-full.txt" href={href("/llms-full.txt")} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={ogType} />
    {Astro.site && <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site)} />}
    {ogImage && <meta property="og:image" content={ogImage} />}
    {ogImage && <meta property="og:image:width" content="1200" />}
    {ogImage && <meta property="og:image:height" content="630" />}
    <meta name="twitter:card" content="summary_large_image" />
    {ogImage && <meta name="twitter:image" content={ogImage} />}
    {Astro.site && <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />}
    <slot name="head" />
    <script is:inline>
      // Runs before paint to avoid a light-mode flash on dark-theme visits.
      const stored = localStorage.getItem("theme");
      const dark = stored ? stored === "dark" : matchMedia("(prefers-color-scheme: dark)").matches;
      document.documentElement.classList.toggle("dark", dark);
    </script>
  </head>
  <body class="flex min-h-full flex-col bg-white font-sans text-zinc-800 antialiased dark:bg-zinc-950 dark:text-zinc-200">
    <Header />
    <div class="flex-1">
      <slot />
    </div>
    <Footer />
  </body>
</html>
