# Housekeeping routine for the n8n-decanter repo

A periodic maintenance pass: backlog hygiene, docs/changelog currency, release
check, worktree/branch prune, dependency triage, CI/test green, drift audits.

**The routine itself lives in the root `AGENTS.md` → "Housekeeping routine"** —
read and follow that section. It is the tool-agnostic single source of truth
(per `CLAUDE.md` → "Agent tooling"); this file is only a pointer so the pass is
invocable as `/housekeeping`.
